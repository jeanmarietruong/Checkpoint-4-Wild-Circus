{% extends 'base.html.twig' %}

{% block title %}CircusAct{% endblock %}

{% block body %}
    <h1>CircusAct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circus_act.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ circus_act.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ circus_act.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('circus_act_index') }}">back to list</a>

    <a href="{{ path('circus_act_edit', {'id': circus_act.id}) }}">edit</a>

    {{ include('circus_act/_delete_form.html.twig') }}
{% endblock %}
